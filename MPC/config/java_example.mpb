// -*- MPC -*-
// $Id:$

project : java_only {
    libpaths -= .
    specific (ospljavamake, javabat) {
        class_dir = classes<%slash%>
        genflags =
        // compile_flags =
        // targetoutdir =
    }
    specific (ospljavamake) {

        // Switch off dependency checking entry generation
        supports_include =
    }
    //staticname = *
}
