const char * $(type_name)_metaDescriptor[] = {"$(meta-descriptor)"};
const int  $(type_name)_metaDescriptorArrLength = $(meta-descriptorArrLength);
const int  $(type_name)_metaDescriptorLength = $(meta-descriptorLength);
