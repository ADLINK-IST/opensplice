Prerequisites, assumptions, constraints:

    This test uses application in osplo/testsuite/dbt/api/shared. It must be built
    before running this test.

HOWTO RUN:
    Using STAXMon tool:
        1. Start STAXMon.
        2. Select to execute the "dds2361_run_test.xml" script.

    From command line:
        1. Source STAF environment varibale script (STAFEnv).
        2. Run the "dds2361_run_test.sh" script.

    Test logs for the test are in "log" sub-folders for each sub-testcase.